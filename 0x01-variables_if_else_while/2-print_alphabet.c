#include <stdio.h>

/**
 * main - lowercase
 *
 * Description: using the main function
 * this program prints the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
	char c;
	for(c = 'a' ; ch <= 'z' c++)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
