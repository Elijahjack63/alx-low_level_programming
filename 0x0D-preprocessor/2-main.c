#include <stdio.h>

/**
 * main- prints the name of the file follow by a new line
 * print the name of the program
 *
 * Return: Always success
 */
int main(void)
{
	printf("%s\n", _FILE_);

	return (0);
}
